# Level 6 Dossier Workflow Trigger

This file intentionally triggers the `TriMatrix Level 6 Dossier` workflow after the workflow file was added to `main`.

Purpose:

```text
Generate verifier outputs and assemble level6_dossier in a real GitHub Actions job.
```

Boundary:

```text
This trigger does not by itself prove production deployment, external security audit, or long-term operations.
```
